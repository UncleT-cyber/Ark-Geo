"""Per-domain ARK specialist identity contracts (identity/reasoning/tools).

Data files, not code — composed at runtime by
:mod:`app.agent.context_builder`. Domain keys match ``SpecialistSpec.domain``.
"""
